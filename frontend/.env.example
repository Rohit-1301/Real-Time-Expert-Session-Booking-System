# Base URL for all REST API calls (proxied by Vite in dev, direct URL in production)
VITE_API_URL=http://localhost:5000

# Socket.io server URL (must point directly at the backend — cannot be proxied)
VITE_SOCKET_URL=http://localhost:5000
